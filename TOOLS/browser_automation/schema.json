[
  {
    "type": "function",
    "function": {
      "name": "scrape_webpage",
      "description": "Navigate to a URL using a headless browser and pull visible page text.",
      "parameters": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "The web address to navigate to."
          }
        },
        "required": ["url"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "fill_web_form",
      "description": "Automate filling out form inputs on a target webpage.",
      "parameters": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "Target webpage URL containing the form."
          },
          "form_data": {
            "type": "string",
            "description": "JSON string mapping CSS selectors to text values (e.g., '{\"#username\": \"user\"}')."
          },
          "submit_selector": {
            "type": "string",
            "description": "Optional CSS selector for the submit button."
          }
        },
        "required": ["url", "form_data"]
      }
    }
  }
]